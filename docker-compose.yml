services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      
      - /var/run/docker.sock:/var/run/docker.sock
      
      - ./submissions:/app/submissions
    environment:
      
      - HOST_SUBMISSIONS_DIR=C:/Users/patha/Desktop/sensei_demo/IICPC2/submissions